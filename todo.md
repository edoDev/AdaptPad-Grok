# Todo list for AdaptPad

-   find replace
-   mode to change between pretty format and "code" backend side so you can see the make changes underneath, this might look like a view button next to export that changes the view to the same options as raw export?
-   paragraph mode from the test formatting drop down isn't working maybe also add "normal". I assume paragraph not formatting properly is another theme style being opened and not closed properly. 
-   export table did not format correctly.
-   ability to convert pictures to text. 
-   add code block option
-   add print button
-   add ability to send to grok
-   add ability to send to claude
-   add ability to send to github
-   add ability to post to social media story
-   auto scale pages adding 5 carriage returns to bottom of page always. 
-   make width of page wider
-   narrow margins by default
-   add box for file name at top
-   when clicking export, and no file name is set, default to first 60 characters of top line of document
-   this might be a bad idea... but make a share link, since there is no save, the entire document will be in the share link url. (RIP text messages)
